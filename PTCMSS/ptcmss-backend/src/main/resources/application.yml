spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ptcmss_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: none  # Disable DDL auto (DB already exists with correct schema)
    show-sql: true
    properties:
      hibernate:
        # Use physical naming strategy to match database column names exactly
        physical_naming_strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  mail:
    host: smtp.gmail.com
    port: 587
    username: trongho.373664@gmail.com
    password: hxyobazohcxbcmjx
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  profiles:
    active: dev

openapi:
  service:
    api-docs: Computer_sell
    server: http://localhost:8080
    title: Transport Application
    version: 1.0.0

server:
  port: 8080

management:
  endpoints:
    web.exposure.include: health,info



